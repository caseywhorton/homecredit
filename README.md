# README

# Usage

Navigate to the scripts folder and execute:  
`python3 train.py`

This pulls data from application_train.csv.

Update the parameter YAML file with the features to train with, be sure to update the `preprocessing.py` module.
